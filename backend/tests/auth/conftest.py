"""Auth-scope-specific fixtures (currently empty; the shared `make_token` /
`hs256_secret` live in `backend/tests/conftest.py` so they're available
across `tests/auth/`, `tests/api/new/`, and `tests/api/integration/`)."""
